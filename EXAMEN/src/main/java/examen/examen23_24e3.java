/*
 * A partir de la clase Prenda, aplicar los siguientes cambios:

a) Crear un atributo stockPrendas de tipo long que se actualice sumando uno, cada vez que se instancia la clase Prenda.
b) Crear un atributo tipo de tipo String que se establezca a null si el valor introducido en el constructor no coincide con alguno de los siguientes:
falda, pantalon, jersey.
c) Crear un atributo referencia que se establezca de forma automatica generando un numero aleatorio de 5 cifras, cada vez que se instancia la clase Prenda.
d) Crear un atributo double precioConIva
e) Modificar el metodo setPrecioConIva() para que establezca el valor del atributo precioConIva a partir de los atributos iva y precioSinIva. Este método sólo se podrá llamar desde el constructor de la clase
F) Crea un constructor que reciba parametros atributos: tipo, talla, sexo, precioSinIva e Iva, e inicialice correctamente todos los atributos de la clase, incluido precioConIva
g) Crear otr constructor que reciba como parámetros tipo y precioSinIva, establezca el atributo talla a 42, el atributo sexo a H y el atributo iva a 21.0 por defecto, e inicialice correctamente el resto de atributos
h) Crea el metodo toString que muestre el valor de todos los atributos de la clase
i) En un  nuevo fichero Java, haz una clase ejecutable llamada TestPrenda y en ella:
-Crea un nuevo objeto de la clase Prenda con cada uno de los dos constructores definidos
-Imprime por pantalla el valor de los atributos de los dos objetos haciendo uso para ello el metdoo toString()

 */
package examen;

import java.util.Scanner;

/**
 *
 * @author javier.gonzalezprado
 */
public class examen23_24e3 {

    public static void main(String[] args) {
        Scanner teclado = new Scanner(System.in);

        
        
    }

}
